/**
 * Save All Required Parameters For Diffrent Setup : such as DB ,Server or JsonWebToken
 */
const config = {
    PORT: 80,
    AUTH_SECRET: 'secret',
    PATH_TO_FILES: './uploads/',
    SERVER_LOCATION: 'Europe/Frankfurt',
    APP_TIMEZONE: 'Asia/Jerusalem'
}

module.exports = config;